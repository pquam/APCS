

checkpoint2 {


  main {
  
    Scanner console = new scanner(System.in);
    System.out.println("What are your memes?");
    String memes = "";
    while (!memes.equals("quit")) {
      memes = console.nextline();
      produceAnswer(memes);
      }
      
      /*
      while (memes != "quit") {
      memes = console.nextline();
      produceAnswer(memes);
      }
      */
    
  }
  
  produceAnswer(String memes) {
    
    scanner parser = new scanner(memes).usedelimiter(" ");
      operand1 =  parser.next();
      operator1 = parser.next();
      operand2 = parser.next();
      parser.close();
      
      
      
  }
}
